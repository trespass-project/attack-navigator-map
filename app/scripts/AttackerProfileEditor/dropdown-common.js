'use strict';

const React = require('react');
const MenuItem = require('react-bootstrap').MenuItem;

module.exports = {
	caret: (<span className='caret'></span>),
	divider: (<MenuItem divider={true}></MenuItem>),
};
